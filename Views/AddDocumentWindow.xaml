<Window x:Class="CarRentalSystem_WPF.Views.AddDocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Width="480"
        SizeToContent="Height"
        MinHeight="200" MinWidth="380"
        WindowStartupLocation="CenterOwner"
        Background="White"
        FontFamily="Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#F8FAFC"
                BorderBrush="#E5E7EB"
                BorderThickness="0,0,0,1"
                Padding="24,20">
            <StackPanel>
                <TextBlock Text="{Binding WindowTitle}" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                <TextBlock Text="Rental contract" FontSize="12" Foreground="#9CA3AF" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form fields -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,20,24,4">

                <TextBlock Text="CAR" Style="{StaticResource FieldLabel}"/>
                <ComboBox Style="{StaticResource FilterCombo}"
                          ItemsSource="{Binding Cars}"
                          SelectedItem="{Binding SelectedCar}"
                          Margin="0,0,0,16">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} {1} ({2})">
                                        <Binding Path="Brand"/>
                                        <Binding Path="Model"/>
                                        <Binding Path="Year"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="RENTER" Style="{StaticResource FieldLabel}"/>
                <ComboBox Style="{StaticResource FilterCombo}"
                          ItemsSource="{Binding Renters}"
                          SelectedItem="{Binding SelectedRenter}"
                          Margin="0,0,0,16">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} — {1}">
                                        <Binding Path="FullName"/>
                                        <Binding Path="PhoneNumber"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Dates side by side -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="START DATE" Style="{StaticResource FieldLabel}"/>
                        <Border Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                                CornerRadius="8" Height="40">
                            <DatePicker BorderThickness="0" Background="Transparent"
                                        Padding="4,0" VerticalContentAlignment="Center"
                                        FontSize="13" FontFamily="Segoe UI"
                                        SelectedDate="{Binding StartDate}"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="END DATE" Style="{StaticResource FieldLabel}"/>
                        <Border Background="White" BorderBrush="#D1D5DB" BorderThickness="1"
                                CornerRadius="8" Height="40">
                            <DatePicker BorderThickness="0" Background="Transparent"
                                        Padding="4,0" VerticalContentAlignment="Center"
                                        FontSize="13" FontFamily="Segoe UI"
                                        SelectedDate="{Binding EndDate}"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <TextBlock Text="TOTAL COST" Style="{StaticResource FieldLabel}"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="$" FontSize="26" FontWeight="SemiBold" Foreground="#6B7280"
                               VerticalAlignment="Center" Margin="0,0,2,0"/>
                    <TextBlock Text="{Binding TotalCost}" FontSize="34" FontWeight="Bold"
                               Foreground="#111827" VerticalAlignment="Center"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                BorderBrush="#E5E7EB"
                BorderThickness="0,1,0,0"
                Padding="24,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Style="{StaticResource GhostBtn}"
                        Width="90" Margin="0,0,8,0"
                        Command="{Binding CancelCommand}"/>
                <Button Content="Remove"
                        Style="{StaticResource DangerBtn}"
                        Width="90" Margin="0,0,8,0"
                        Command="{Binding RemoveCommand}"
                        Visibility="{Binding ShowRemoveButton}"/>
                <Button Content="{Binding ButtonText}"
                        Style="{StaticResource PrimaryBtn}"
                        Width="90"
                        Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
